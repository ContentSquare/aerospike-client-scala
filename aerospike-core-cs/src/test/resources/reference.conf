aerospike {

    clientpolicy {
        user = "root"
        password = null
        clusterName = null
        maxConnsPerNode = 100

        eventloop {
            threadNumbers = 1
            eventLoopType = "NETTY_NIO"
        }
    }
}