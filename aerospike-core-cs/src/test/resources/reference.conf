aerospike {
    hosts = ""
    clientPolicy {
        user = "root"
        password = "password"
        clusterName = "cluster"
        maxConnsPerNode = 300
        eventLoop {
            threadNumbers = 10
            eventLoopType = "DIRECT_NIO"
        }
    }
}